[project]
name = "audiomi"
version = "0.1.0"
description = "Realtime loopback/mic audio capture and TCP broadcast utility."
readme = "readme.md"
requires-python = ">=3.12"
dependencies = [
    "numpy==2.2.6",
    "pyinstaller>=6.16.0",
    "python-dotenv>=1.2.1",
    "scipy>=1.16.2",
    "soundcard==0.4.5",
]
